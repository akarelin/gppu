# GPRU — General Purpose Rusty Utilities (ЖПRU)

`v3.5.0-alpha.1` — Rust migration track with manually-triggered CI (fmt, clippy, cross-platform build/test, packaging sanity).

Release tag target: `v3.5.0alpha1` on `dev` branch.

```text
  _ _  ____  ____  _  _ 
/( ( \/    \(  _ \/ )( \
)    () /\ ( )   /) \/ (
\_)_)/\_)(_/(__\_)\____/
```

> Rust port of `gppu`. The legacy Python codebase lives at the repository root; this `rust/` directory contains the Cargo workspace for the in-progress Rust migration.

See [RUST_PORT.md](RUST_PORT.md) and [GPRU_LLM_TESTING.md](GPRU_LLM_TESTING.md).
