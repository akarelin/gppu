"""gppu.ymro — the Y2 init→load→start→stop lifecycle (_YMRO, _YInit, _YLoad, _YStart, YStepper, mixin_Stepper),
unchanged from gppu/app.py. Only Y2 uses it; where it lives is Alex's decision (see README)."""
